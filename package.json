{
  "name": "lotion",
  "private": true,
  "version": "0.1.0",
  "description": "Director Mode: SVG → storyboard → Lottie with Disney physics.",
  "packageManager": "npm@10.9.0",
  "scripts": {
    "dev:backend": "npm --workspace @lotion/backend run dev",
    "build:plugin": "npm --workspace @lotion/figma-plugin run build",
    "typecheck": "npm --workspace @lotion/backend run typecheck && npm --workspace @lotion/figma-plugin run typecheck && npm --workspace @lotion/shared run typecheck",
    "test": "npm --workspace @lotion/shared run test && npm --workspace @lotion/backend run test"
  },
  "workspaces": [
    "apps/*",
    "figma-plugin",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
