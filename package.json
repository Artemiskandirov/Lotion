{
  "name": "lotion",
  "private": true,
  "version": "0.1.0",
  "description": "AI-assisted Lottie feasibility and motion planning for Figma assets.",
  "packageManager": "npm@10.9.0",
  "scripts": {
    "dev": "npm --workspace @lotion/web run dev",
    "build": "npm --workspace @lotion/web run build",
    "build:plugin": "npm --workspace @lotion/figma-plugin run build",
    "typecheck": "npm --workspace @lotion/web run typecheck && npm --workspace @lotion/figma-plugin run typecheck && npm --workspace @lotion/shared run typecheck"
  },
  "workspaces": [
    "apps/*",
    "figma-plugin",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
